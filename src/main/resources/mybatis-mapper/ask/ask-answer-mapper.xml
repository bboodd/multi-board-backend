<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hh.multiboarduserbackend.domain.ask.answer.AskAnswerRepository">
    <sql id="askAnswerColumns">
          content
        , created_date
        , delete_yn
    </sql>

    <!--    문의 pk 에 해당하는 답변 리스트 조회    -->
    <select id="findAllByPostId" parameterType="long" resultType="com.hh.multiboarduserbackend.common.vo.CommentVo">
        SELECT
              <include refid="askAnswerColumns"></include>
            , (SELECT nickname FROM admin WHERE admin.admin_id = ask_answer.admin_id) AS nickname
        FROM
            ask_answer
        WHERE
            ask_post_id = #{askPostId}
            AND delete_yn = 0
        ORDER BY
            ask_answer_id ASC
    </select>
</mapper>